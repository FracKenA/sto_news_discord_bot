# Environment file for Docker Compose
# Copy this to .env and update with your actual values

# Discord Bot Token (required)
DISCORD_TOKEN=YOUR_DISCORD_BOT_TOKEN_HERE
# Replace with your actual Discord bot token
# You can obtain this token from the Discord Developer Portal
# https://discord.com/developers/applications
# Make sure to keep this token secret and do not share it publicly
# If you don't have a bot token, create a new application in the Discord Developer Portal,
# then create a bot user and copy the token from there.
# If you are using Docker, you can set this token as an environment variable

# Optional configuration overrides
POLL_PERIOD=600
POLL_COUNT=20
FRESH_SECONDS=600
MSG_COUNT=10
DATABASE_PATH=/data/stobot.db

# Go logging level (can be set via LOG_LEVEL environment variable)
# Options: panic, fatal, error, warn, info, debug, trace
LOG_LEVEL=info
